name: Release

on:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  check-version:
    name: Verify tag matches pubspec version
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
    steps:
      - uses: actions/checkout@v4

      - id: version
        name: Extract pubspec version
        run: |
          VERSION=$(grep '^version:' pubspec.yaml | sed 's/^version:[[:space:]]*//; s/+.*//')
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "pubspec version: $VERSION, tag: ${GITHUB_REF_NAME}"

      - name: Check tag ↔ pubspec sync
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          if [ "v$VERSION" != "${GITHUB_REF_NAME}" ]; then
            echo "::error::Tag ${GITHUB_REF_NAME} does not match pubspec version $VERSION."
            echo "Update pubspec.yaml 'version:' (or re-tag) so they match."
            exit 1
          fi

  build-android:
    name: Android APK
    runs-on: ubuntu-latest
    needs: check-version
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Install dependencies
        run: flutter pub get

      - name: Build APKs (release, split per ABI)
        run: flutter build apk --release --split-per-abi

      - name: Stage artifacts
        run: |
          VERSION="${{ needs.check-version.outputs.version }}"
          mkdir -p dist
          cp build/app/outputs/flutter-apk/app-arm64-v8a-release.apk \
            "dist/Filmify-$VERSION-android-arm64-v8a.apk"
          cp build/app/outputs/flutter-apk/app-armeabi-v7a-release.apk \
            "dist/Filmify-$VERSION-android-armeabi-v7a.apk"
          cp build/app/outputs/flutter-apk/app-x86_64-release.apk \
            "dist/Filmify-$VERSION-android-x86_64.apk"

      - uses: actions/upload-artifact@v4
        with:
          name: android-apk
          path: dist/*

  build-linux:
    name: Linux build
    runs-on: ubuntu-latest
    needs: check-version
    steps:
      - uses: actions/checkout@v4

      - name: Install Linux desktop dependencies
        run: |
          sudo apt-get update
          # libmpv-dev/libepoxy-dev: pkg-config deps of media_kit_video's
          # plugin (PkgConfig::mpv / PkgConfig::epoxy). libmpv2 ships the
          # runtime soname used when bundling libmpv into the artifacts.
          sudo apt-get install -y libgtk-3-dev liblzma-dev ninja-build \
            libmpv-dev libmpv2 libepoxy-dev

      - uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Install dependencies
        run: flutter pub get

      - name: Build Linux (release)
        run: flutter build linux --release

      - name: Bundle libmpv into the app bundle
        run: |
          # Make the portable tar.gz self-contained: copy the system libmpv
          # soname next to the binary (the bundle's RPATH resolves it from
          # lib/ first). deb/rpm additionally declare the distro dependency.
          cp -a /usr/lib/x86_64-linux-gnu/libmpv.so.2* \
            build/linux/x64/release/bundle/lib/

      - name: Install fpm
        run: sudo gem install fpm --no-document

      - name: Package deb and rpm
        run: |
          VERSION="${{ needs.check-version.outputs.version }}"
          mkdir -p dist
          STAGE=$(mktemp -d)
          mkdir -p "$STAGE/opt/filmify" \
                   "$STAGE/usr/share/applications" \
                   "$STAGE/usr/share/icons/hicolor/512x512/apps"
          cp -r build/linux/x64/release/bundle/. "$STAGE/opt/filmify/"
          cp packaging/linux/filmify.desktop "$STAGE/usr/share/applications/"
          cp assets/branding/linux/filmify-512.png \
             "$STAGE/usr/share/icons/hicolor/512x512/apps/filmify.png"

          fpm -s dir -t deb -n filmify -v "$VERSION" -a amd64 \
            --description "Bilingual movie discovery & download hub" \
            --url "https://github.com/mst-ghi/filmify" \
            --maintainer "mst-ghi" \
            --category video \
            --depends "libgtk-3-0" \
            --depends "libstdc++6" \
            --depends "libmpv2" \
            --depends "libepoxy0" \
            -C "$STAGE" \
            -p "dist/Filmify-$VERSION-linux-amd64.deb" .

          fpm -s dir -t rpm -n filmify -v "$VERSION" -a x86_64 \
            --description "Bilingual movie discovery & download hub" \
            --url "https://github.com/mst-ghi/filmify" \
            --maintainer "mst-ghi" \
            --depends "gtk3" \
            --depends "libstdc++" \
            --depends "mpv-libs" \
            --depends "epoxy" \
            -C "$STAGE" \
            -p "dist/Filmify-$VERSION-linux-x86_64.rpm" .

          rm -rf "$STAGE"

      - name: Stage portable tar.gz
        run: |
          VERSION="${{ needs.check-version.outputs.version }}"
          mkdir -p dist "dist/Filmify-$VERSION-linux-x64"
          cp -r build/linux/x64/release/bundle/. "dist/Filmify-$VERSION-linux-x64/"
          tar -czf "dist/Filmify-$VERSION-linux-x64.tar.gz" -C dist "Filmify-$VERSION-linux-x64"
          rm -rf "dist/Filmify-$VERSION-linux-x64"

      - uses: actions/upload-artifact@v4
        with:
          name: linux-build
          path: dist/*

  build-windows:
    name: Windows build
    runs-on: windows-latest
    needs: check-version
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Install dependencies
        run: flutter pub get

      - name: Build Windows (release)
        run: flutter build windows --release

      - name: Build installer (Inno Setup)
        shell: pwsh
        run: |
          $iscc = "C:\Program Files (x86)\Inno Setup 6\ISCC.exe"
          if (-not (Test-Path $iscc)) {
            choco install innosetup -y --no-progress
            $iscc = "C:\Program Files (x86)\Inno Setup 6\ISCC.exe"
          }
          New-Item -ItemType Directory -Force -Path dist | Out-Null
          & $iscc "/DAppVersion=${{ needs.check-version.outputs.version }}" "packaging\windows\filmify.iss"

      - name: Stage portable zip
        shell: pwsh
        run: |
          $version = "${{ needs.check-version.outputs.version }}"
          New-Item -ItemType Directory -Force -Path stage | Out-Null
          Copy-Item -Recurse -Force build/windows/x64/runner/Release/* "stage/"
          Compress-Archive -Path stage/* -DestinationPath "dist/Filmify-$version-windows-x64.zip" -Force
          Remove-Item -Recurse -Force stage

      - uses: actions/upload-artifact@v4
        with:
          name: windows-build
          path: dist/*

  release:
    name: Publish GitHub release
    runs-on: ubuntu-latest
    needs: [check-version, build-android, build-linux, build-windows]
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: List artifacts
        run: ls -la dist

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          name: Filmify ${{ needs.check-version.outputs.version }}
          files: dist/*
          generate_release_notes: true
